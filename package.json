{
  "name": "www",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "node-sass": "^7.0.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && echo amazyyy.com > ./build/CNAME && cp README.md ./build",
    "eject": "react-scripts eject",
    "precommit": "",
    "test": "standard ./src"
  },
  "lint-staged": {
    "*.js": [
      "standard --fix ./src",
      "git add"
    ]
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@amazingandyyy/git-hooks": "^0.1.0",
    "lint-staged": "^10.2.11",
    "standard": "^14.3.4"
  }
}
